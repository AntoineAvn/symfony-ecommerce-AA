{% extends 'base.html.twig' %}

{% block title %}Product index{% endblock %}

{% block body %}
    <h1 class="d-flex justify-content-center mt-5">Products</h1>
        {# <a href="{{ path('app_product_create') }}" class="d-flex justify-content-center mt-2">Create new</a> #}

    <div class="card-wrapper d-flex justify-content-center mt-3 flex-wrap">
        {% include "include/card.html.twig" %}
    </div>

    <div class="d-flex justify-content-center mt-5 mb-5">{{ knp_pagination_render (products) }}</div>
    
{% endblock %}