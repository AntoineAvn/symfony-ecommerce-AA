{% extends 'base.html.twig' %}

{% block title %}Product{% endblock %}

{% block body %}
    <h1 class="d-flex justify-content-center mt-5">{{ product.name }}</h1>

<div class="card-wrapper d-flex justify-content-center mt-5">
    <div class="card-show w-75 mb-3">
        <img class="card-img-top" src="{{ product.image }}" alt="Card image cap">
        <div class="card-body">
            <h5 class="card-title text-primary">{{ product.category.name }}</h5>
            <p class="card-text mt-2">{{ product.description }}</p>
            <div class="d-flex justify-content-end"><a href="#" class="btn btn-secondary">Acheter</a></div>
        </div>
    </div>
</div>

   {#  <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ product.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ product.name }}</td>
            </tr>
            <tr>
                <th>Excerpt</th>
                <td>{{ product.excerpt }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ product.description }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ product.image }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ product.quantity }}</td>
            </tr>
            <tr>
                <th>Sold</th>
                <td>{{ product.sold }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ product.price }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ product.status }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ product.createdAt ? product.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdateAt</th>
                <td>{{ product.updateAt ? product.updateAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table> #}

    <a href="{{ path('app_products') }}">back to list</a>

    <a href="{{ path('app_product_update', {'id': product.id}) }}">Update</a>

    {{ include('content/product/_delete_form.html.twig') }}
{% endblock %}
